# commit

Commit message format and rules for whenever committing, saving, or pushing changes.

## Skills

- `commit` — one logical change per commit, no `Co-Authored-By` trailer, and submodule pointer bumps are committed in the parent repo too.
